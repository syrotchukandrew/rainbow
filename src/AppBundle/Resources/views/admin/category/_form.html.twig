{{ form_start(form) }}
{{ form_widget(form) }}

<input type="submit" value="{{ button_label|default('Удалить') }}"
       class="{{ button_css|default("btn btn-primary") }}"/>

<a href="{{ path('admin_categories') }}" class="btn btn-primary">
    Назад к списку
</a>
{{ form_end(form) }}
