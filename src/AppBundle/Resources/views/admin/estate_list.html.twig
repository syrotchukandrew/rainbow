{% extends 'AppBundle:admin:base_admin.html.twig' %}

{% block body %}
    <div class="wrapper">
    <div id="page-wrapper">
    <h1>Объекты недвижимсоти</h1>
    <table class="table table-striped">
        <thead>
        <tr>
            <th>Название</th>
            <th>Фото</th>
            <th><i class="fa fa-calendar"></i>Создан</th>
            <th><i class="fa fa-cogs"></i>Действия</th>
        </tr>
        </thead>
        <tbody>
        {% for estate in pagination %}
            <tr>
                <td>{{ estate.title }}</td>
                <td>
                   {# {% if estate.imageName %}
                        <img class="img-responsive" src={{ asset("#{post.imageName}") | imagine_filter('my_admin') }} alt="foto" style="float: left">
                    {% endif %}#}
                </td>
                <td>{{ estate.createdAt.date|date("d F Y H:i:s") }}</td>
                <td>
                    <div class="item-actions">
                       {# <a href="{{ path('admin_post_show', { slug: post.slug }) }}" class="btn btn-sm btn-default">
                            Show post </a>
                        <a href="{{ path('admin_post_edit', { slug: post.slug }) }}" class="btn btn-sm btn-primary">
                            <i class="fa fa-edit"></i> Edit post </a>#}
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>
    </div>
    </div>
{% endblock body %}