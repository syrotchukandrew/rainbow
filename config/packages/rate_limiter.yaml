framework:
    rate_limiter:
        livesearch:
            policy: sliding_window
            limit: 30
            interval: '1 minute'
        api_public:
            policy: sliding_window
            limit: 60
            interval: '1 minute'
        password_reset:
            policy: sliding_window
            limit: 5
            interval: '15 minutes'
