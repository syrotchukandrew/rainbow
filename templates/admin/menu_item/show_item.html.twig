{% extends 'admin/base_admin.html.twig' %}
{% block panel %}
    <h3>{{ item.title }}</h3>
    <p>{{ item.description }}</p>
    {{ include('admin/menu_item/_form.html.twig', {
        form: delete_form,
        button_label: 'Удалить',
        button_css: 'inline-flex items-center px-3 py-2 border border-gray-300 rounded text-sm text-gray-700 bg-white hover:bg-gray-50 cursor-pointer',
        show_confirmation: true,
    }, with_context = false) }}
{% endblock panel %}