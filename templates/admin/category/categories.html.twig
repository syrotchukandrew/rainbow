{% extends 'admin/base_admin.html.twig' %}
{% block panel %}
    <h1>Категорії</h1>
    <div class="item-actions">
        <a href="{{ path('admin_category_root_new') }}" class="btn btn-sm btn-default">
            <i class="fa fa-plus"></i> Додати кореневу категорію</a>
        <a href="{{ path('admin_category_new') }}" class="btn btn-sm btn-default">
            <i class="fa fa-plus"></i> Додати категорію</a>
    </div>
    <div id="react-admin-categories"></div>
{% endblock panel %}
